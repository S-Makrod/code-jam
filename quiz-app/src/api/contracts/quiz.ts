export type Score = number

export type Topics = Array<string>

export type Answer = {
    correct: boolean
}

export type Guess = {
    answer: string
}

export type PlayerId = {
    playerId: string
}